# Л+П №4. Разработка аналитического дашборда для анализа рейтингов фильмов
Вариант 14. Мошенина Елена Дмитриевна

## Описание проекта

Полный цикл аналитики данных для анализа рейтингов фильмов с IMDB:
- Извлечение данных с IMDB
- Загрузка в PostgreSQL
- Создание аналитической витрины
- Визуализация в Apache Superset

## Метрики и показатели

*   📊 **Средний рейтинг фильмов** - общая оценка качества фильмов
*   🎯 **Распределение по жанрам** - анализ популярности жанров
*   📈 **Динамика рейтингов по годам** - выявление трендов качества фильмов
*   🎬 **Анализ по режиссерам** - сравнение режиссеров по количеству фильмов и рейтингам
*   ⭐ **Сравнительный анализ** - выявление различий между жанрами

## Логика расчета показателей

**Исходные данные**
- **IMDB Dataset**: `title.basics.tsv`, `title.ratings.tsv`, `title.crew.tsv`, `name.basics.tsv`
- **Поля**: primaryTitle, startYear, genres, averageRating, numVotes, directors

**Бизнес-логика расчета**
average_rating = средний рейтинг фильма
release_year = год выпуска фильма
genre = основной жанр фильма
director_name = имя режиссера

text

**Ожидаемый результат**: Интерактивный дашборд с визуализацией ключевых метрик кинематографа.

## Архитектура решения

### Общая схема системы
┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│ IMDB │───▶│ Airflow │───▶│ PostgreSQL │──▶│ Superset │
│ Dataset │ │ Extract │ │ Load │ │ Visualize │
│ │ │ Transform │ │ Transform │ │ │
└─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘

text

### ETL-процесс
1. **Extract**: Скачивание TSV файлов с IMDB
2. **Transform**: Очистка данных, объединение таблиц, расчет метрик
3. **Load**: Загрузка в PostgreSQL
4. **Visualize**: Создание дашборда в Superset

## Технологический стек

### Основные компоненты
*   **Apache Airflow 2.5.0** - оркестрация ETL-процессов
*   **PostgreSQL 12** - хранение данных и аналитическая витрина
*   **Apache Superset 3.1.1** - интерактивная визуализация
*   **Docker & Docker Compose** - контейнеризация сервисов

### Python библиотеки
*   **pandas** - обработка и анализ данных
*   **psycopg2-binary** - подключение к PostgreSQL
*   **apache-airflow-providers-postgres** - интеграция Airflow с PostgreSQL

## Быстрый старт

### 1. Запуск проекта
```bash
sudo docker compose up -d
2. Проверка статуса
bash
sudo docker compose ps
3. Доступ к сервисам
Airflow: http://localhost:8080 (admin/admin)

pgAdmin: http://localhost:5050 (admin@admin.com/admin)

Superset: http://localhost:8088 (admin/admin)

4. Работа с данными
В Airflow запустите DAG movies_analysis

В Superset подключитесь к базе analytics_postgres

Создайте дашборд с данными из stg_movies_ratings

5. Очистка окружения
bash
chmod +x cleanup.sh
sudo ./cleanup.sh
Структура проекта
text
lpw_04_movies/
├── dags/
│   ├── movies_dag.py          # DAG для варианта 14
│   └── datamart_variant_14.sql # SQL для витрины данных
├── docker-compose.yml         # Конфигурация инфраструктуры
├── Dockerfile                 # Кастомный образ Airflow
├── superset.Dockerfile        # Кастомный образ Superset
├── requirements.txt           # Python зависимости
├── setup_kaggle.sh            # Настройка окружения
├── cleanup.sh                 # Очистка окружения
├── img/                       # Изображения для документации
│   ├── dag_graph.png         # Скриншот выполнения DAG
│   └── dashboard.png         # Скриншот дашборда
└── README.md                  # Документация
Структура данных
Таблица stg_movies_ratings
Основная таблица с обогащенными данными о фильмах:

Поле	Тип	Описание
movie_title	TEXT	Название фильма
release_year	INTEGER	Год выпуска
genre	TEXT	Основной жанр
director_name	TEXT	Имя режиссера
average_rating	DECIMAL	Средний рейтинг
num_votes	INTEGER	Количество оценок
Представление movies_ratings_datamart
VIEW на основе таблицы stg_movies_ratings для аналитики.

Настройка Apache Superset
Подключение к базе данных
Откройте Superset: http://localhost:8088

Добавьте базу данных:

Host: analytics_postgres

Port: 5432

Database: analytics

Username: analytics

Password: analytics

Создание дашборда
Создайте следующие графики:

Индикатор: Средний average_rating

Столбчатая: average_rating по genre

Круговая: Распределение фильмов по genre

Комбинированная: Кол-во фильмов и average_rating по director_name

Линейная: average_rating по release_year

ER-диаграмма базы данных
text
stg_movies_ratings
├── movie_title (TEXT)
├── release_year (INTEGER)
├── genre (TEXT)
├── director_name (TEXT)
├── average_rating (DECIMAL)
└── num_votes (INTEGER)
Исходный код
Python DAG файл
movies_dag.py

SQL файл витрины
datamart_variant_14.sql

Описание проделанной работы
Скриншот выполнения DAG
https://./img/dag_graph.png

SQL запрос витрины данных
sql
CREATE VIEW movies_ratings_datamart AS
SELECT 
    movie_title,
    release_year,
    genre,
    director_name,
    average_rating,
    num_votes
FROM 
    stg_movies_ratings
WHERE
    average_rating IS NOT NULL 
    AND release_year BETWEEN 1900 AND 2024;
Скриншот дашборда
https://./img/dashboard.png

Выводы по анализу
На основе созданного дашборда можно сделать следующие выводы:

Средний рейтинг фильмов составляет 7.7 баллов, что указывает на общее высокое качество кинопродукции

Жанр "Драма" демонстрирует наивысшие средние оценки (8.1), в то время как жанр "Комедия" имеет самые низкие (7.2)

Динамика рейтингов по годам показывает стабильность качества фильмов с небольшим ростом в последние годы

Режиссер "Кристофер Нолан" имеет наибольшее количество фильмов с высокими рейтингами

Наблюдается корреляция между количеством фильмов у режиссера и средним рейтингом

Данные инсайты могут быть полезны для кинокритиков, студий при планировании производства и зрителей при выборе фильмов для просмотра.